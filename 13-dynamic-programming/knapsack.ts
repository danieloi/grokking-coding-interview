export function findMaxKnapSackProfit(
  capacity: number,
  weights: number[],
  values: number[]
) {
  // your code will replace this placeholder return statement
  const profits = [];

  for (let i = 0; i < values.length; i++) {
    const value = values[i];
    for (let j = capacity; j > 0; ) {
        if(weights[i])
    }
  }
}
